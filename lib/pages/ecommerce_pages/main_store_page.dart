import 'package:flutter/material.dart';

class MainStorePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Main Store Page')),
    );
  }
}